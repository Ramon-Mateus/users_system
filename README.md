# users_system

Sistema em Django que pega usuários cadastrados no banco de dados juntamente com os arquivos ligados a esses usuários e cria um zip para cada um contendo:

- JSON (dados do usuário)
- Arquivos (foto e currículo)
